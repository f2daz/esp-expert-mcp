[project]
name = "esp-expert-mcp"
version = "0.1.0"
description = "MCP-Server für ESP32/ESP8266-Firmwareentwicklung: Pin-Prüfung, Partitionstabellen, Panic-Decoder, sdkconfig, Serial-Ports, Live-Versionen, ESPHome-Lint."
requires-python = ">=3.11"
license = "MIT"
license-files = ["../LICENSE"]
dependencies = [
    "mcp>=2.2,<3",
    "pyserial>=3.5",
    "pyyaml>=6.0",
]

[project.scripts]
esp-expert-mcp = "esp_expert_mcp.server:main"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/esp_expert_mcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
