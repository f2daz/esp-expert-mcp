# Abhängigkeiten aus der ESP Component Registry (components.espressif.com).
# Versionen pinnen; aktuelle Stände mit `idf.py add-dependency "<name>^x.y"` eintragen lassen.
dependencies:
  idf: ">=5.3"
  # espressif/led_strip: "^3"
